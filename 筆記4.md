啟用aws bedrock 連接 dify agent換AI模型  
透過dify 建立空白應用\>聊天流簡單跟AI進行對話,AI的記憶次數是有限的  
可以預先輸入前提讓AI知道些許資訊,也可以事後修改  
透過dify 建立空白應用\>工作流 來寫程式,可以交由AI來做判斷來引導到其他項目  
AI有時候問題的回答會產生幻覺 (AI 幻覺（AI Hallucination）是指**人工智慧（特別是大型語言模型，如 ChatGPT、Gemini）產出了與真實世界不符、邏輯錯誤或完全虛構的回應，但文字內容卻看似合理且語氣極度自信**。 )  
所以可以建立資料庫事先把資料餵給AI,使其的回答更準確,以後寫程式的判斷可以交給AI來做判斷

AWS VPC 

## **專案背景與目標**

**VPC 名稱：** `dogdoghero` (`vpc-0e6071ac21ba4f1de`)

**VPC CIDR：** `10.17.0.0/16`

**架構目標：** 建立一個同時具備**公有網段**（對外服務）與**多個可用區私有網段**（高可用、隔離安全）的標準雲端網路環境。

### **第一階段：基礎元件建立**

1. **建立網際網路閘道 (Internet Gateway)：**  
   * 建立了名為 `dogdoghero-gw` 的 IGW。  
   * 順利**附加 (Attach)** 到 `dogdoghero` VPC，為整個網路打開對外的通道。  
2. **建立公有路由表 (Public Route Table)：**  
   * 手動建立獨立路由表：`dogdoghero-public-rt`。

### **第二階段：子網路 (Subnet) 規劃與高可用配置**

為了避免網址衝突，並確保跨可用區（AZ）的高可用性，共切分了 4 個 `/24` 的子網段：

### **第三階段：路由與關聯安全設定（關鍵步驟）**

####   **公有網段設定 (Public Subnets)**

1. **編輯公有路由表 (`dogdoghero-public-rt`)：**  
   * 新增一條外網路徑：目的地 `0.0.0.0/0`  目標指向 `dogdoghero-igw`。  
2. **子網路關聯：**  
   * 將 `subnet-az-a` 與 `subnet-az-c` 綁定至此路由表。  
   * *結果：這兩個網段正式成為公有子網路，裡面的機器（配有 Public IP 者）可與外網雙向互通。*

####   **私有網段設定 (Private Subnets)**

1. **編輯私有路由表 (`dogdoghero-private-rt`)：**  
   * **嚴格保持乾淨**，只留預設的內部路由：`10.17.0.0/16 -> local`。  
   *  *絕對不可出現 `0.0.0.0/0 -> igw-xxx`。*  
2. **子網路關聯：**  
   * 將新建立的 `subnet-az-a-2` 與 `subnet-az-c-2` 綁定至此路由表。  
   * *結果：這兩個網段成為完全隔離的私有子網路，外網完全無法直接訪問。*

##  **架構驗證檢查點 (Resource Map)**

設定完成後，從 VPC 資源對應圖確認以下邏輯連線：

* **子網路層：** 4 個子網路正確對應到對應的可用區，無網段衝突。  
* **路由表層：** \* 公有子網路  連接 `dogdoghero-public-rt`  
  * 私有子網路  連接 `dogdoghero-private-rt`  
* **網路連線層：** `dogdoghero-public-rt` 的右側已正確亮起並連線到 **`dogdoghero-igw`**。

##  **日後擴充建議 (Next Steps)**

如果未來放在私有網段（例如 `subnet-az-a-2`）的伺服器或資料庫需要**主動連到外網下載套件或更新系統**，需要補上以下操作：

1. 在公有子網路中建立一個 **NAT Gateway**。  
2. 前往私有路由表 (`dogdoghero-private-rt`)，新增一條 `0.0.0.0/0` 路由並指向該 NAT Gateway。

